using UnityEngine;
using System.Collections;

public class Notes {


    /*
     
    // Base Classes
    BasePanelController: base of all panels

    // Interface
    LanguageInterface


    PanelListController: used to control the panel flows
        List<BasePanelController> panelList

        - AddPanel (BasePanelController panel)
            -> display panel at the top of the screen
        - RemovePanel () 
            -> hides top panel and displays the next panel
        - HideSecondLastPanelDelay (float delay)
            -> exactly as the name suggests
        - HideAllPanels ()
            -> exactly as the name suggests
        - SetUserNotLoggedInMode ()
            -> hides all panels and display the LoginPanel
        - SetUserLoggedInMode ()
            -> hides all panels and display the HomePanel
































    */

}
